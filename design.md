# 日程管理软件 - 设计文档
## 1. 项目概述

- 项目名称： myschedule - 日程管理软件
- 开发语言： C++
- 开发环境： Linux / GCC / CMAKE
- 作者： [马博睿]
- 完成日期： 2026年7月24日

## 2. 功能列表

### 2.1 账户管理
- 用户名 + 密码登录
- 密码使用哈希值存储，不保存明文

### 2.2 任务管理
- 添加任务（任务名称、时间、优先级、分类）
- 查看任务（按时间排序）
- 删除任务（按ID删除）

### 2.3 文件持久化
- 任务数据自动保存到本地文件
- 程序启动时自动加载

### 2.4 交互模式
- 支持命令行一次性执行
- 支持 `run` 交互式命令行界面

### 2.5 多线程提醒
- 后台线程每10秒检查一次任务
- 任务到期时自动打印提醒

## 3. 类设计

### 3.1 Task 类

#### 成员变量
| 变量名 | 类型 | 说明 |
|:---|:---|:---|
| id | int | 任务唯一ID，自动递增 |
| name | std::string | 任务名称 |
| startTime | std::time_t | 任务开始时间 |
| priority | Priority | 优先级（高/中/低） |
| category | Category | 分类（学习/娱乐/生活） |
| remindTime | std::time_t | 提醒时间 |
| reminded | bool | 是否已提醒（防止重复提醒） |

#### 主要方法
| 方法 | 说明 |
|:---|:---|
| Task(name, startTime, priority, category) | 构造函数，自动分配ID |
| getId() / getName() / ... | Getter方法 |
| toFileString() | 将任务转为字符串（用于保存） |
| fromFileString() | 从字符串恢复任务（用于加载） |
| isReminded() / setReminded() | 提醒状态管理 |

## 4. 程序流程

### 4.1 程序启动流程
1. 用户执行 `./myschedule` 命令
2. 程序提示输入用户名和密码
3. 验证密码（比较密码的哈希值）
4. 验证通过后，从 `tasks.txt` 加载任务
5. 根据命令行参数执行相应操作

### 4.2 添加任务流程
1. 用户输入 `addtask <任务名> <时间>`
2. 程序创建 Task 对象（自动分配ID）
3. 将任务添加到任务列表
4. 自动保存到文件

### 4.3 删除任务流程
1. 用户输入 `deltask <ID>`
2. 程序在任务列表中查找对应ID
3. 找到后删除任务
4. 自动保存到文件

### 4.4 提醒流程
1. 程序启动时创建后台线程
2. 线程每隔10秒检查所有任务
3. 如果任务未提醒且开始时间已到
4. 打印提醒信息，并标记为已提醒

## 5. 关键技术说明

### 5.1 密码加密
- 使用 C++ 标准库的 `std::hash<std::string>`
- 只存储密码的哈希值，不存储明文密码
- 验证时比较哈希值

### 5.2 多线程提醒
- 使用 `std::thread` 创建后台线程
- 线程独立于主程序运行
- 使用 `std::this_thread::sleep_for` 控制检查间隔
- 主线程与后台线程共享任务列表（通过引用传递）

### 5.3 文件持久化
- 使用 `std::ifstream` / `std::ofstream` 读写文件
- 每个任务保存为一行，用逗号分隔字段
- 程序启动时从文件恢复任务列表

### 5.4 命令行解析
- 使用 `argc` 和 `argv` 获取命令行参数
- 支持 `addtask`、`showtask`、`deltask`、`run` 等命令
- 交互模式下用 `std::getline` 读取用户输入

## 6. 测试说明

### 6.1 测试脚本
- 使用 `test.sh` 自动化测试脚本
- 测试内容包括：
  - 编译测试
  - 登录测试（正确密码）
  - 添加任务测试
  - 显示任务测试
  - 删除任务测试
  - 交互模式测试

### 6.2 测试结果
所有测试通过，程序运行正常。

## 7. 运行界面

### 7.1 登录界面

### 7.2 添加任务

### 7.3 显示任务

### 7.4 删除任务

### 7.5 任务提醒

## 8. 总结与心得

### 8.1 项目总结
本次项目实现了一个完整的命令行日程管理软件，涵盖了账户管理、任务增删查改、文件持久化、多线程提醒等核心功能。项目采用 C++ 编写，使用 CMake 构建，具有良好的模块化结构。

### 8.2 学习心得
通过本次项目，我掌握了以下内容：
- C++ 类设计与面向对象编程
- 文件读写与数据持久化
- 多线程编程 (`std::thread`)
- Git 版本管理与代码协作
- 命令行工具开发

### 8.3 遇到的困难与解决
1. 多线程提醒功能的调试：通过添加日志输出和逐步排查，最终解决了线程同步问题。
2. Git 推送权限问题：通过配置 SSH 密钥解决。
3. 哈希存储密码：使用 `std::hash` 实现密码加密。

### 8.4 改进方向
- 支持更丰富的时间格式解析
- 添加图形界面
- 支持语音提醒或音乐播放
